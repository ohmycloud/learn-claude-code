[project]
name = "learn-claude-code"
version = "0.1.0"
description = "learn how agent works."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.84.0",
    "dotenv>=0.9.9",
    "httpx[socks]>=0.28.1",
]
